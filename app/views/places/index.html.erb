<h1>My Travel Log</h1>
<ul>
  <% @places.each do |place| %>
    <li>
      <a href="<%= place_path(place) %>"><%= place.name %></a>
    </li>
  <% end %>
</ul>

<a href="<%= new_place_path %>">Add a New Place</a>
